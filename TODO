* virtual species
* add a Field constraint: species division  (derangements)
* functor composition
* recursively defined species   (trees etc.)
* generation of unlabelled structures
* move 'subset' (and 'ksubset'?) into species class 
    so we can specialize generation (?)